name: scale_monk
description: A Flutter plugin to display Ads from ScaleMonk
version: 0.4.1+1
homepage: https://github.com/thize/scale_monk

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lint: ^1.3.0

flutter:
  plugin:
    platforms:
      # android:
      #   package: com.scale_monk
      #   pluginClass: ScaleMonkPlugin
      ios:
        pluginClass: ScaleMonkPlugin