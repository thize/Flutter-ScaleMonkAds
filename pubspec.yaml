name: scale_monk
description: A Flutter plugin to display Ads from ScaleMonk
version: 1.0.0+2
homepage: https://github.com/thize/flutter_scale_monk

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lint: ^1.5.2

flutter:
  plugin:
    platforms:
      # android:
      #   package: com.scale_monk
      #   pluginClass: ScaleMonkPlugin
      ios:
        pluginClass: ScaleMonkPlugin
